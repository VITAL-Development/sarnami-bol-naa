node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo
playwright-report/
e2e/test-results/
test-results.json
.claude/

# /server is tracked directly in this repo (see issue #52 for the plan to
# split it into its own repo once other /server work has landed).
server/node_modules
server/dist
